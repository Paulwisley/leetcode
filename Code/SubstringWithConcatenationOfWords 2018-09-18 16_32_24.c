#include "iostream"
#include "vector"
#include "string"
using namespace std;

vector<int> findSubstring(string s, vector<string>& words) {
        
}

void createWords(vector<int>&used, vector<string>& words,vector<string>&newwords, int &count,string &word){
	if(count == words.size())
	{
		newwords.push_back(word);
		return;
	}
	else {
		for(int i = 0; i < words.size(); i++ ){
			
		}
	}
}

int main(){
	return 0;
}